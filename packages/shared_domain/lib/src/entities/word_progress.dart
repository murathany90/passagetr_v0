class WordProgress {
  const WordProgress({
    required this.wordId,
    required this.mastery,
    required this.seenCount,
  });

  final String wordId;
  final int mastery;
  final int seenCount;
}
